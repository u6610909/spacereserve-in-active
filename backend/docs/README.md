# Docs

- [api.md](api.md) — full API reference (request/response examples for every endpoint)
- [peer-api.md](peer-api.md) — the FinderAI peer API contract, both directions

The architecture diagram and ERD live in the root [README.md](../../README.md) as Mermaid diagrams
rather than a separate `ERD.png` — GitHub renders Mermaid natively, so it stays in sync with the
schema instead of going stale as a static image.
