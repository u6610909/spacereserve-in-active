# SpaceReserve — วิธีทำงานร่วมกัน

ส่งให้บดินทร์กับวรชัยอ่าน

Repo: `https://github.com/u6610909/spacereserve`

---

## ก่อนเริ่ม

**เปิด Claude Code ในโฟลเดอร์โปรเจกต์เท่านั้น**

ในโฟลเดอร์มีไฟล์ `CLAUDE.md` อยู่ ข้างในเขียนไว้หมดว่าโปรเจกต์นี้ทำอะไร ใช้อะไร ห้ามทำอะไร
ถ้าเปิดในโฟลเดอร์ Claude ของทุกคนจะอ่านไฟล์นี้เหมือนกัน โค้ดที่ได้ก็จะไปทางเดียวกัน

ถ้าเปิดที่อื่น Claude จะเดาเอง แล้วโค้ดของเราสามคนจะไม่เหมือนกันเลย ต้องมานั่งแก้ทีหลัง

---

## ทำไมแบ่งงานแบบนี้

อิทธิ์กับบดินทร์ต้องทำ senior project ไปพร้อมกันด้วย
เลยให้วรชัยรับงานวิชานี้ก้อนใหญ่สุด ส่วนอิทธิ์เก็บเฉพาะงานที่ต้องใช้บัญชีกับเซิร์ฟเวอร์ของตัวเอง

สัดส่วนงานที่เหลือ (เฟส 1 ทำเสร็จไปแล้ว)

| คน | สัดส่วน | เฟส |
|---|---|---|
| **วรชัย** | ~55% | 2, 4, 7, 8, 9, 10, 13, 14 |
| **บดินทร์** | ~25% | 5, 6 |
| **อิทธิ์** | ~20% | 3, 12 + สมัครบัญชีทั้งหมด + รีวิวกับ merge ทุก PR |

---

## ใครทำอะไร

แบ่งกันตามโฟลเดอร์ ถ้าแก้คนละโฟลเดอร์ โค้ดจะไม่ชนกัน ถ้าแก้ไฟล์เดียวกันเมื่อไหร่ ชนแน่นอน

### อิทธิ์
`src/config/` · `src/lib/` · `src/middleware/errorHandler.ts` · `src/middleware/validate.ts` ·
`src/middleware/notFound.ts` · `Dockerfile` · `docker-compose*.yml` · `.github/workflows/` ·
`nginx/` · `deploy.sh`

**เฟส 3 (Key Vault) กับ 12 (เอาขึ้นเซิร์ฟเวอร์)** และงานที่คนอื่นทำแทนไม่ได้

- สร้าง Azure Key Vault, service principal, และฐานข้อมูล Postgres
- สร้าง Entra tenant กับ app registration แล้วส่ง client ID กับ secret ให้วรชัย
- สมัคร Gemini กับ SendGrid แล้วเอา key ไปเก็บใน Key Vault
- รีวิวกับ merge ทุก PR
- ทุกอย่างที่เกี่ยวกับเซิร์ฟเวอร์ คนอื่นห้าม SSH เข้าเด็ดขาด

### วรชัย
`prisma/` · `src/modules/auth/` · `src/middleware/requireAuth.ts` · `src/middleware/requireRole.ts` ·
`src/middleware/requireApiKey.ts` · `src/modules/search/` · `src/integrations/` ·
`src/modules/external/` · `src/modules/admin/` · `docs/`

**เฟส 2 (ฐานข้อมูล), 4 (ระบบล็อกอิน), 7 (Gemini), 8 (SendGrid), 9 (เปิด API ให้เพื่อนกลุ่มอื่น),
10 (เรียก API ของเพื่อนกลุ่มอื่น), 13 (เอกสาร), 14 (เตรียมอัดวิดีโอ)**

### บดินทร์
`src/modules/rooms/` · `src/modules/reservations/`

**เฟส 5 (ห้อง) กับ 6 (การจอง)**

เฟส 6 เป็นหัวใจของระบบเลย — เช็คว่าเวลาจองทับกันไหม, คนเกินความจุห้องไหม, ใครยกเลิกได้บ้าง,
เจ้าหน้าที่ขอห้องคืนได้ยังไง และเป็นส่วนที่ต้องเขียนเทสต์เยอะสุด

### ทุกคน
เฟส 11 — เขียนเทสต์ของส่วนตัวเอง

---

## ไฟล์ที่ใช้ร่วมกัน — ตรงนี้ชนกันบ่อย

**`package.json` / `package-lock.json`** ทุกเฟสแตะไฟล์นี้หมด กฎมี 2 ข้อ

1. ก่อนเปิด PR ให้ใช้ `git pull --rebase origin main` (ใช้ rebase อย่าใช้ merge)
2. ถ้า `package-lock.json` ชนกัน **อย่านั่งแก้เอง** ให้เอาของ main มาแล้วสร้างใหม่

```bash
git checkout --theirs package-lock.json
npm install
git add package-lock.json
```

เพราะถ้าแก้มือ จะได้ไฟล์ที่ลงไลบรารีคนละชุดกับที่เทสต์ไว้

**`.env.example`** เป็นของอิทธิ์ แต่วรชัยเพิ่มชื่อ key ใหม่ได้ตอนเฟส 8–10 (ใส่ค่าว่างไว้)
แค่ห้ามลบหรือเปลี่ยนชื่อของเดิม

**`README.md`** อิทธิ์ดูแลตารางรหัสไปก่อน พอถึงเฟส 13 วรชัยรับไปทั้งไฟล์
แต่ห้ามลบตารางรหัสทิ้ง

---

## ลำดับงาน — อ่านก่อนเริ่ม

**เฟส 3a ต้องมาก่อน เป็นงานอิทธิ์ ใช้เวลาครึ่งวัน**

จริง ๆ เฟส 2 เริ่มก่อนไม่ได้ เพราะต้องใช้ไฟล์ `src/lib/prisma.ts` กับค่า `config.databaseUrl`
ซึ่งอยู่ในโฟลเดอร์ของอิทธิ์ อิทธิ์เลยต้องทำสองอันนี้ให้ก่อน (ยังไม่ต้องมี Key Vault)
แล้วเฟส 2 ถึงจะเริ่มได้จริง ส่วน Key Vault ตัวเต็มค่อยตามมาทีหลัง

**จากนั้นวรชัยทำเฟส 2 ทันที ก่อนงานอื่นทั้งหมดของตัวเอง**

เพราะทุกคนต้องรอไฟล์ `prisma/schema.prisma` ทุกโมดูลต้องใช้ Prisma client
ถ้ายังไม่มี บดินทร์เริ่มไม่ได้เลย และเฟส 4 ของวรชัยเองก็เริ่มไม่ได้ด้วย

schema เขียนไว้ให้ครบแล้วใน `MASTER_PROMPT.md` หัวข้อ 4 ก็อปไปใช้ได้เลย
แล้วสร้าง migration กับ seed ต่อ ใช้เวลาวันเดียวก็เสร็จ ไม่ต้องถึงอาทิตย์

พอเฟส 2 merge แล้ว ทั้งสามคนทำพร้อมกันได้เลย

เฟส 4 (ล็อกอิน) เป็นเฟสที่ยากสุด วรชัยควรเริ่มทันทีหลังเฟส 2 อย่าดองไว้ถึงกันยา
เพราะ OIDC มักพังด้วยเรื่องงี่เง่า เช่น URL มีขีดเกินมาอันนึง
ไม่มีใครอยากไปเจอตอนอาทิตย์สุดท้าย

---

## ไฟล์ที่มีแต่อิทธิ์แก้ได้

อยากแก้ไฟล์พวกนี้ **ทักบอกก่อน อย่าแก้เอง**

- `src/app.ts` — ลำดับในไฟล์นี้สลับไม่ได้ สลับแล้วพัง
- `src/config/index.ts`
- `docker-compose*.yml`, `Dockerfile`, `deploy.sh`, `.github/workflows/`
- `nginx/`
- `CLAUDE.md`, `MASTER_PROMPT.md`, ไฟล์นี้

นอกจากนี้ ในโฟลเดอร์ตัวเองแก้ได้เลย

---

## วิธีใช้ git

1. ห้าม commit ลง `main` ตรง ๆ ให้แตก branch ใหม่ตามเฟส เช่น `phase-5-rooms`
2. ก่อนเริ่มงาน กับก่อนเปิด PR ให้ pull `main` มาก่อน
3. เปิด PR แล้วรอ CI ผ่าน อิทธิ์เป็นคน merge
4. ข้อความ commit เขียนแบบนี้ `feat: add room availability filter`

`main` ตั้งล็อกไว้แล้ว ต้องผ่าน PR และ CI เท่านั้น push ตรงเข้าไปไม่ได้

---

## ห้ามทำเด็ดขาด

บอก Claude ของตัวเองด้วย

1. **ห้ามรัน `prisma migrate dev`** มันลบข้อมูลทิ้งได้ ให้สร้าง migration ในเครื่องแล้วใช้ `prisma migrate deploy`
2. **ห้าม commit ไฟล์ `.env`** ถ้าต้องใช้รหัสอะไร ถามอิทธิ์
3. **`process.env` เขียนได้แค่ในโฟลเดอร์ `src/config/`** ถ้าเขียนที่อื่น PR จะไม่ผ่าน
4. **ห้าม SSH เข้าเซิร์ฟเวอร์** ให้อิทธิ์คนเดียว เครื่องนั้นมีงานวิชาอื่นอยู่ด้วย พังทีกระทบหมด
5. **ห้ามยุ่งกับ `/content` กับ `/api`** ใน Nginx
6. **Controller เขียนสั้น ๆ** งานจริงอยู่ใน service และมีแต่ service ที่คุยกับ Prisma
7. **ใช้ zod ตรวจทุกอย่างที่รับมาจากผู้ใช้** ทั้ง body, query, param
8. Claude อยากลงไลบรารีที่ไม่มีในลิสต์ **ถามก่อน**

---

## ถ้าติด

ทักในกลุ่มก่อน อย่าเพิ่งให้ Claude หาทางออกเอง

ที่จะกินเวลาเราจริง ๆ ไม่ใช่บั๊ก แต่เป็นสองคนทำของเดียวกันคนละแบบ
หรือ Claude ของใครสักคนไปแก้ไฟล์ที่ไม่ใช่ของตัวเอง
