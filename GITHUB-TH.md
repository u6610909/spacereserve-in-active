# งานของอิทธิ์ กับ วิธีใช้ GitHub

ส่งให้บดินทร์กับวรชัย

---

## อิทธิ์ทำอะไรในโปรเจกต์นี้

ผมเขียนโค้ดน้อยสุดในทีม (~20%) แต่เป็นคนดูแลของที่คนอื่นทำแทนไม่ได้

**1. สมัครและถือรหัสทุกอย่าง**

Azure Key Vault, ฐานข้อมูล Postgres, Entra tenant กับ app registration (สำหรับระบบล็อกอิน),
Gemini, SendGrid ทั้งหมดอยู่ในบัญชี Azure ของผม

**ถ้าต้องใช้รหัส key หรือ URL อะไร — ทักหาผม อย่าไปสมัครเอง**
เพราะของพวกนี้ต้องเก็บใน Key Vault ที่เดียว ถ้าต่างคนต่างสมัคร เดี๋ยวสับสน

**2. เอาโปรเจกต์ขึ้นเซิร์ฟเวอร์ (เฟส 12)**

เซิร์ฟเวอร์ตัวนี้มีงานแล็บกับ WordPress ของวิชาอื่นรันอยู่ด้วย พังทีคือกระทบหลายวิชา
เพราะงั้น **ห้ามใครก็ตาม SSH เข้าเซิร์ฟเวอร์** ผมคนเดียว

**3. Key Vault (เฟส 3)**

ระบบเก็บรหัสผ่านทั้งหมด

**4. รีวิวกับ merge ทุก PR**

นี่คืองานหลักจริง ๆ ของผม

เราสามคนใช้ Claude คนละตัว แล้ว Claude แต่ละตัวจะเขียนโค้ดคนละสไตล์
ผมเป็นคนอ่านทุก PR ว่าตรงกับสเปกใน `CLAUDE.md` ไหม
ถ้าไม่ตรงผมจะส่งกลับไปแก้ ไม่ได้จะจับผิด แต่ถ้าปล่อยไป เดือนกันยาจะได้แก้กันทั้งคืน

**5. ตามงานคนนอก**

ตามอาจารย์เรื่อง Key Vault กับ app registration และตามกลุ่ม FinderAI เรื่อง API ของเขา

---

## วิธีใช้ GitHub

### ครั้งแรกครั้งเดียว

**1. กดรับ invite**

เช็คอีเมลที่ผูกกับ GitHub จะมี invite เข้า repo `spacereserve` กดรับก่อน ไม่งั้น clone ไม่ได้

**2. โหลดโปรเจกต์ลงเครื่อง**

```bash
git clone https://github.com/u6610909/spacereserve.git
cd spacereserve
npm install
```

**3. เปิด Claude Code ในโฟลเดอร์นี้**

สำคัญมาก ถ้าเปิดที่อื่น Claude จะไม่เห็นไฟล์ `CLAUDE.md` แล้วจะเดาเอาเอง

---

### ทุกครั้งที่จะเริ่มงานใหม่

**1. ดึงของใหม่ลงมาก่อนเสมอ**

```bash
git checkout main
git pull
```

ข้อนี้คนลืมบ่อยสุด ถ้าไม่ pull แล้วเขียนทับของเก่า เดี๋ยวโค้ดชนกัน

**2. แตก branch ใหม่**

```bash
git checkout -b phase-5-rooms
```

ชื่อ branch ตั้งตามเฟสที่ทำ เช่น `phase-2-database`, `phase-4-auth`, `phase-6-reservations`

**3. ทำงานตามปกติ**

**4. เซฟขึ้น GitHub**

```bash
git add .
git commit -m "feat: add room availability filter"
git push -u origin phase-5-rooms
```

ข้อความ commit เขียนแบบนี้
`feat:` = เพิ่มของใหม่ · `fix:` = แก้บั๊ก · `chore:` = งานจิปาถะ

**5. เปิด Pull Request**

เข้าเว็บ GitHub จะมีปุ่มเหลือง ๆ ขึ้นมาว่า "Compare & pull request" กดเลย
ใส่ชื่อกับคำอธิบายสั้น ๆ ว่าทำอะไรไป แล้วกด Create

**6. รอ CI**

GitHub จะรันเช็คโค้ดให้อัตโนมัติ ประมาณ 30 วิ

- ✅ เขียว = ผ่าน ทักบอกผมมา merge ให้
- ❌ แดง = ไม่ผ่าน กดเข้าไปดูว่าพังตรงไหน แก้ แล้ว push ขึ้น branch เดิม CI จะรันใหม่เอง

**7. หลังผม merge แล้ว**

```bash
git checkout main
git pull
```

แล้วแตก branch ใหม่สำหรับงานถัดไป

---

## Pull Request คืออะไร

คือการบอกว่า *"ผมทำงานเสร็จในสำเนาของผมแล้ว ช่วยตรวจแล้วเอาไปรวมกับตัวจริงหน่อย"*

เราไม่เขียนโค้ดใส่ตัวจริง (`main`) ตรง ๆ เพราะสามคนเขียนพร้อมกันแล้วจะทับกัน
แต่ละคนไปทำในสำเนาของตัวเอง (branch) เสร็จแล้วค่อยขอรวมเข้ามา

ผมล็อก `main` ไว้แล้ว push ตรงเข้าไปไม่ได้ ต้องผ่าน PR อย่างเดียว
ไม่ได้ไม่ไว้ใจกัน แต่กัน Claude ของใครสักคนเผลอลบงานคนอื่นตอนตีสอง

---

## ถ้าไม่ถนัดพิมพ์คำสั่ง

โหลด **GitHub Desktop** มาใช้ได้ ทำได้ทุกอย่างข้างบนแบบกดปุ่ม
branch, commit, push, เปิด PR ครบ

---

## ที่พลาดกันบ่อย

| พลาด | ผลที่ตามมา |
|---|---|
| ลืม `git pull` ก่อนเริ่มงาน | โค้ดชนกัน เสียเวลาแก้ |
| ทำงานบน `main` | push ไม่ได้ ต้องมาย้ายงานทีหลัง |
| commit ไฟล์ `.env` | รหัสหลุดขึ้นเน็ต ต้องเปลี่ยนรหัสใหม่หมด |
| แก้ไฟล์ของคนอื่น | PR โดนตีกลับ |
| ให้ Claude ลงไลบรารีใหม่เอง | PR โดนตีกลับ ถามก่อน |

---

## สรุปสั้น ๆ

```
git checkout main
git pull
git checkout -b phase-X-ชื่องาน
   ... ทำงาน ...
git add .
git commit -m "feat: ..."
git push -u origin phase-X-ชื่องาน
   ... เปิด PR บนเว็บ รอ CI เขียว ทักผม ...
```

ติดตรงไหนทักในกลุ่มได้เลย ถามดีกว่าเดา
